import { FilterOnQuestionsPipe } from './filter-on-questions.pipe';

describe('FilterOnQuestionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOnQuestionsPipe();
    expect(pipe).toBeTruthy();
  });
});
