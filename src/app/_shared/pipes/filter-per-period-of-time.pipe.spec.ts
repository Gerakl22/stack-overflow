import { FilterPerPeriodOfTimePipe } from './filter-per-period-of-time.pipe';

describe('FilterPerPeriodOfTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPerPeriodOfTimePipe();
    expect(pipe).toBeTruthy();
  });
});
