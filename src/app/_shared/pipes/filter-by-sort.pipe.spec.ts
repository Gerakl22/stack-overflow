import { FilterBySortPipe } from './filter-by-sort.pipe';

describe('FilterBySortPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySortPipe();
    expect(pipe).toBeTruthy();
  });
});
